# Hardware docs

> One `*.md` per hardware repo listed in `project.yaml`, **generated by `/understand-hardware`**
> (full-depth, workflow-backed review). These are Claude's reference for what each board *is and is not*.
> The hardware repos themselves are **read-only** — reviewed in place, never modified.

```mermaid
flowchart LR
  HW["hardware repo (read-only)"] --> UH["/understand-hardware"]
  UH --> MD["docs/hardware/&lt;name&gt;.md"]
```

No reviews have run yet. Run `/understand-hardware` to populate this folder.
