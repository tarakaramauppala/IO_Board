---
description: Full-depth review of each firmware repo → docs/firmware/<name>.md (read-only reference; never edit firmware)
---

Run the **understand-firmware** skill at [.claude/skills/understand-firmware/SKILL.md](../skills/understand-firmware/SKILL.md) and follow it exactly.

Remember the hard stop: firmware repos are READ-ONLY references — clone shallow into `.refs/`, read, produce docs; never modify firmware anywhere. Use a fan-out workflow for true full depth.

$ARGUMENTS
