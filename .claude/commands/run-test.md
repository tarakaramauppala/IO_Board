---
description: Flash the board and run a bespoke test plan — observe, judge pass/fail, record results, route failures to /triage
---

Run the **run-test** skill at [.claude/skills/run-test/SKILL.md](../skills/run-test/SKILL.md) and follow it exactly.

Confirm the use case/plan, DUT serial, and firmware image (a run flashes the unit). Execute the plan's scripts (flash → observe → judge), write results/, honor the false-fail caveats from memory, then route failures to /triage.

$ARGUMENTS
