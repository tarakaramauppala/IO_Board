---
description: Full-depth review of each hardware repo → docs/hardware/<name>.md (read-only; workflow-backed)
---

Run the **understand-hardware** skill at [.claude/skills/understand-hardware/SKILL.md](../skills/understand-hardware/SKILL.md) and follow it exactly.

Remember the hard stop: the hardware repos are READ-ONLY — clone them shallow into the gitignored `.refs/`, read, and produce docs; never modify, commit, or push them. Use a fan-out workflow for true full depth.

$ARGUMENTS
