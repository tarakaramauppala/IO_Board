---
description: Build a bespoke test plan + scripts for one use case under tests/<use-case>/ (no shared framework)
---

Run the **build-test-plan** skill at [.claude/skills/build-test-plan/SKILL.md](../skills/build-test-plan/SKILL.md) and follow it exactly.

Pick a use case, read its sharpened definition + the relevant docs + memory, then build `tests/<use-case>/` (plan.md + scripts) using whatever tools fit — nrfjprog/J-Link, pyserial/RTT, the PPK2 lib, cloud API. Ground every signal/limit in the docs.

$ARGUMENTS
